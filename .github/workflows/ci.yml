name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - release/**
  workflow_dispatch:

env:
  DATABASE_URL: postgresql://inner_avatar:inner_avatar_dev@localhost:5432/inner_avatar?sslmode=disable
  AUTH_SECRET: ci-auth-secret
  OPENAI_API_KEY: ""
  NEXT_PUBLIC_APP_URL: http://localhost:3000
  INNER_AVATAR_WEB_URL: http://localhost:3000
  NEXT_PUBLIC_ADMIN_URL: http://localhost:3001

jobs:
  verify:
    name: Verify app builds and tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: inner_avatar
          POSTGRES_USER: inner_avatar
          POSTGRES_PASSWORD: inner_avatar_dev
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U inner_avatar -d inner_avatar"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "yarn"

      - name: Enable Corepack
        run: corepack enable

      - name: Verify Yarn
        run: yarn --version

      - name: Install dependencies
        run: yarn install --immutable

      - name: Validate environment template
        run: yarn check:env

      - name: Validate Prisma schema
        run: yarn prisma validate

      - name: Apply database migrations
        run: yarn db:migrate:deploy

      - name: Typecheck
        run: yarn typecheck

      - name: Lint
        run: yarn lint

      - name: Run auth tests
        run: yarn test:auth

      - name: Run web tests
        run: yarn test:web

      - name: Run admin tests
        run: yarn test:admin

      - name: Run AI tests
        run: yarn test:ai

      - name: Run RAG evals
        run: yarn test:rag

      - name: Run internal RAG smoke
        run: yarn smoke:rag

      - name: Run pilot evals
        run: yarn test:pilot

      - name: Run founder calibration checks
        run: |
          yarn test:founder-calibration
          yarn test:founder-calibration-regression
          yarn report:founder-calibration-setup
          yarn report:founder-calibration
          yarn report:founder-calibration-comparison

      - name: Run ChatGPT app tests
        run: yarn test:chatgpt

      - name: Build web
        run: yarn build:web

      - name: Build admin
        run: yarn build:admin

      - name: Build ChatGPT app
        run: yarn build:chatgpt

  docker:
    name: Build Docker images
    runs-on: ubuntu-latest
    needs: verify

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "yarn"

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Validate Docker Compose config
        run: yarn docker:compose:config

      - name: Build web image
        run: docker build -f apps/web/Dockerfile -t inner-avatar-web:${{ github.sha }} .

      - name: Build admin image
        run: docker build -f apps/admin/Dockerfile -t inner-avatar-admin:${{ github.sha }} .

      - name: Build ChatGPT app image
        run: docker build -f apps/chatgpt-app/Dockerfile -t inner-avatar-chatgpt-app:${{ github.sha }} .
