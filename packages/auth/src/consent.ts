export {
  hasRequiredPilotConsents,
  OPTIONAL_PILOT_CONSENTS,
  PILOT_CONSENT_VERSION,
  REQUIRED_PILOT_CONSENTS,
  type OptionalPilotConsent,
  type PilotConsent,
  type PilotConsentRecord,
  type RequiredPilotConsent,
} from "@inner-avatar/types/pilot-consent"
